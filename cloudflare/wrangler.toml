name = "http-evolution-benchmark"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# Enable HTTP/3 (enabled by default on Cloudflare)
# No special config needed - Cloudflare handles HTTP/3 automatically

[vars]
ENVIRONMENT = "production"
